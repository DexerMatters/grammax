[package]
name = "grammax"
version = "0.1.0"
edition = "2024"

[dependencies]
internment = { version = "0.8.6", features = ["arena"] }
rustc-hash = "2.1.0"
warp = { version = "0.4.2", optional = true }

[features]
webui = ["dep:warp"]
vsclsp = []